#!/usr/bin/env bash

if command -v starship >/dev/null 2>&1
then
  eval "$(starship init bash)"
else
  echo "starship not found. Please install it via: curl -sS https://starship.rs/install.sh | sh"
fi
