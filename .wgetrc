# Keep network failures bounded without changing request identity, crawler
# policy, output verbosity, remote traversal, or downloaded filenames.
timeout = 60

# Retry a few times when a download fails, but do not use Wget's default of 20.
tries = 3
